[project]
name = "insurance-risk-ml"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
    "fastapi",
    "pydantic",
    "numpy",
    "scikit-learn",
    "joblib",
    "xgboost"
]

[tool.vercel]
entrypoint = "src.api.app:app"